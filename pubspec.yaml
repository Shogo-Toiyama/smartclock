name: smartclock
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  go_router:
  flutter_hooks:
  flutter_riverpod:
  riverpod_annotation:
  cupertino_icons: ^1.0.8
  hooks_riverpod: ^2.5.2
  timezone: ^0.9.4
  device_preview:
  hive:
  hive_flutter:

dev_dependencies:
  build_runner: '^2.4.12'
  riverpod_generator:
  flutter_test:
    sdk: flutter

  flutter_lints: 

flutter:
  assets:
    - assets/images/
  uses-material-design: true